# 价值观推演系统 · 驾驶舱

一句话:这是一个帮我做「价值观分拣 + 反事实推演」的系统——把假装是核心的伪铁律逐个撬开,分清哪些是真正不可谈判的目的本身。

## 五个阶段

0. **价值观分拣** —— 把原料里的每条价值观投进三个抽屉之一(不可动约束 / 待撬靶子 / 自由变量)。
1. **靶子排序** —— 从第二类抽屉挑出待撬的伪核心,按撬动后影响面排队。
2. **反事实构造** —— 为当前靶子构造一个挑战它的反事实情景,并校验它没有越过第一类约束。
3. **现实性检验** —— 用检索/事实去压这个反事实,看它在现实里站不站得住。
4. **对抗推演** —— 对幸存的反事实持续施压,逼出靶子到底是真铁律还是已松动。

## 纪律(每次会话都遵守)

- **【启动纪律】** 每次会话开始,先读 `state/snapshot.md` 和 `core/journal.jsonl` 的最近 10 条,然后用一两句话复述:当前在哪个阶段、三抽屉现状、上次停在哪。等我确认后再继续。
- **【落盘纪律】** 每当发生一次认知转变(我松动一条假设 / 某条价值观从一个抽屉移到另一个 / 一个反事实撬开新东西),立刻向 `core/journal.jsonl` 追加一条记录,不要攒着。
- **【压缩纪律】** 每跑完一个阶段,主动提醒我做一次 handoff(把成果写入 journal 和 snapshot)再继续。
- **【不可变纪律】** `core/` 目录下的文件只追加、永不修改或删除。

## 命名约定

`targets/` 与 `research/` 下的文件用编号前缀一一对应:
- 靶子 `001-xxx.md`
- 检验 `research/001-xxx-findings.md`
- 原始结果 `research/raw/001-xxx-raw.md`

## 当前态快照

> 由 journal 推导,勿手写维护。
